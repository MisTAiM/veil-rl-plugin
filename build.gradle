plugins {
    id 'java'
    id 'checkstyle'
}

repositories {
    mavenLocal()
    maven {
        url = 'https://repo.runelite.net'
        content {
            excludeGroup 'org.lwjgl'
        }
    }
    mavenCentral()
}

def runeLiteVersion = 'latest.release'

dependencies {
    compileOnly group: 'net.runelite', name: 'client', version: runeLiteVersion
    compileOnly 'org.projectlombok:lombok:1.18.30'
    annotationProcessor 'org.projectlombok:lombok:1.18.30'
    implementation 'com.google.code.gson:gson:2.10.1'

    testImplementation(group: 'net.runelite', name: 'client', version: runeLiteVersion) {
        exclude group: 'org.lwjgl'
    }
    testImplementation(group: 'net.runelite', name: 'jshell', version: runeLiteVersion) {
        exclude group: 'org.lwjgl'
    }

    testRuntimeOnly('org.lwjgl:lwjgl:3.3.2') { artifact { classifier = 'natives-windows' } }
    testRuntimeOnly('org.lwjgl:lwjgl-opengl:3.3.2') { artifact { classifier = 'natives-windows' } }
    testRuntimeOnly('org.lwjgl:lwjgl-openal:3.3.2') { artifact { classifier = 'natives-windows' } }
    testRuntimeOnly('org.lwjgl:lwjgl-glfw:3.3.2') { artifact { classifier = 'natives-windows' } }

    testImplementation 'junit:junit:4.12'
    testImplementation 'org.mockito:mockito-core:3.12.4'
    testAnnotationProcessor 'org.projectlombok:lombok:1.18.30'
}

group = 'gg.veil'
version = '3.0.0'

java {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
